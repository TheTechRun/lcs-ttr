# Required auth settings
LCS_USERNAME=change-this
LCS_PASSWORD=change-this
LCS_SECRET_KEY=change-this-to-a-long-random-string

# Optional auth setting (defaults to 30)
# This controls login session lifetime (how long you stay logged in)
LCS_SESSION_EXPIRY_DAYS=30

# Optional app setting
# This controls default auto-delete TTL for NEW snippets/files
# (different from LCS_SESSION_EXPIRY_DAYS which is only for login sessions)
# DEFAULT_EXPIRY options:
# - Presets: 1h, 4h, 1d
# - Custom format: NT where N=number and T in {m,h,d,w,M,y}
#   Examples: 15m, 12h, 3d, 2w, 2M, 1y
# - For no auto-delete by default: leave DEFAULT_EXPIRY unset/commented
# DEFAULT_EXPIRY=4h
